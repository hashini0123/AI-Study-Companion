spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ai_study_companion?createDatabaseIfNotExist=true
    username: root
    password: '1234'
    driver-class-name: com.mysql.cj.jdbc.Driver

  flyway:
    enabled: true
    baseline-on-migration: true
    locations: classpath:db/migration